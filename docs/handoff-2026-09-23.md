# Handoff — 2026-09-23

Session covered three interpreter perf investigations, a bench-API rewrite, CI fixes, and the
post-JIT roadmap triage. `develop` is at `be78f08541`, everything below is pushed.

## The headline: the post-JIT roadmap is DONE

WS-1 through WS-7 are all in `develop`. Two landed this session (WS-2 controller ownership
`69aa5843f8`, WS-4 nearby library sharing `be78f08541`); the rest were already there by earlier
routes. **WS-8 (tvOS first run) is the only unstarted workstream** and the plan calls it the best
effort-to-impact ratio of the eight, largely deletion work.

Fourteen stale branches and ten worktrees were retired. Every deleted tip is recoverable from a
pushed `archive/*` tag (`git checkout archive/agent-ws4-continuity`).

### The redundancy trap — read before merging any old branch

Every remaining workstream branch LOOKED substantial and was entirely superseded. The check that
catches it is per-file, not per-commit:

```bash
for f in $(git diff --name-only $(git merge-base develop $B) $B); do
  git cat-file -e develop:"$f" 2>/dev/null || { echo "ABSENT: $f"; continue; }
  git diff --quiet develop:"$f" $B:"$f" || echo "differs: $f"
done
```

Zero "ABSENT" files means nothing is unshipped. WS-1 was the worked example: 3 of 6 files
byte-identical, and merging it would have REGRESSED three things (restored a controller call WS-2
replaced, removed WS-4's sharing button, un-localized a string). A conflict on these branches means
*stale*, not *pending*.

Also: `SettingsRootView.swift` inflated four branches by ~4400 lines each. `develop` already split
that monolith into 24 files; the branches carry the pre-split copy.

## Bench-sweep fix: LANDED

The rescued fix from `worktree-agent-a1d16119d5ac2e45b` (`9cb6859e9f`) was reviewed, built
(iOS, `Debug (Non-Jailbroken)`, clean) and landed on develop as `fix(bench): reboot the title per
value when sweeping boot-time keys`. `POST /api/bench/sweep` now does a real stop -> boot ->
load-state cycle per value for boot-time keys, and `GET /api/bench/sweep/result` returns the
per-value runs (see `docs/dev/debug-api.md`). Not yet exercised on a device: the first real
boot-time sweep should be watched for the reboot timing out (`rebooted: false` + `error`).
The rescue worktree and its branch can be deleted.

## Open items, roughly in priority order

1. **Sentry re-triage after the next nightly.** Symbolication was broken because the Dolphin core
   compiled with NO `-g` (zero DWARF sections) — fixed `58e1b21a9d`, but only builds made AFTER it
   can symbolicate. The 15 existing issues never will. Top five by users: thread_start (250),
   swtch_pri (148), EXC_BREAKPOINT (106), CachedInterpreter::ExecuteOneBlock (80),
   _dispatch_client_callout (52).
2. **Library sharing UI**, to be more like iFly: automatic discovery/display of available libraries
   rather than manual. Check entitlements land correctly — `Source/iOS/App/Project.swift` is
   Tuist-GENERATED, so edit it and re-run `tuist generate --no-open`. Note the nearby-sharing code
   that just landed enforces exclusions server-side in the manifest builder, not by filtering the
   browse UI.
3. **Pokemon Colosseum (GC6E01) hangs** immediately after its save state loads: 96.7% of cycles in
   a 4-instruction self-branch at `0x80098040` (`nop / li r3,0 / nop / b ->self`), only 94 unique
   blocks executed. Booting it fresh WITHOUT the state took the app down entirely. Not a perf
   problem.
4. **WS-8 tvOS first run.**

## Performance: stop optimizing the interpreter

Three targets, all far under their headline. Details in the memory notes; the short version:

| target | headline | reality |
|---|---|---|
| A gather-pipe stores | 16.5% of cycles | already fast-pathed by `620ef7a993`; fusion measured <1%, inside the error bars |
| B FP/PS generic dispatch | 19.4% | mislabeled ~3x; real tax 5.8%, and every opcode is ALREADY whitelisted — one flag flip |
| C dyn_link 75% hit rate | 25% misses | real: fixed to 99.6%, killing 98.7% of dispatcher round-trips — but only ~+1.1% throughput |

Measured on the four previously-slow titles with the user's flags on, all at full emulated speed and
**0.00% generic dispatch**: F-Zero GX 59.9 vps, NFS Underground 59.5, GoldenEye Rogue Agent 55.4,
Pokemon Colosseum 59.9 (hung). There is no instruction-level headroom left worth chasing. NFS needs
~48% more throughput before its adaptive clock stops underclocking, which only JIT delivers, and JIT
is sideload-only (never App Store).

**Device measurement is the binding constraint**, not ideas: per-leg noise is ~2%, so the standard
error on any A/B comparison is ~1.5% — larger than any remaining candidate win.

## Gotchas worth carrying forward

- `git merge`, do NOT cherry-pick these old branches: their final trees merge cleanly but individual
  commits do not replay.
- `git checkout -- build/xcframework` before EVERY commit; local app builds rewrite the tracked
  slices.
- zsh does not word-split unquoted variables — `set -A arr ...` + `"${arr[@]}"` in loops.
- The bench's `replace` mode resets every key to its DEFAULT first, which silently changed the CPU
  core and later dual-core. Defaults != a sane running config.
